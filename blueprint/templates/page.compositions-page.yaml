kind: Page
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: compositions-page
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    allowedResources:
      - buttons
      - datagrids
    items:
      - resourceRefId: compositions-page-button-drawer-filters
      - resourceRefId: compositions-page-datagrid
  resourcesRefs:
    items:
      - id: compositions-page-button-drawer-filters
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: compositions-page-button-drawer-filters
        namespace: {{ .Release.Namespace }}
        resource: buttons
        verb: GET
      - id: compositions-page-datagrid
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: compositions-page-datagrid
        namespace: {{ .Release.Namespace }}
        resource: datagrids
        verb: GET
        slice:
          page: 1
          perPage: 5