kind: Button
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: compositions-page-button-drawer-filters
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    icon: fa-filter
    type: primary
    size: small
    clickActionId: compositions-page-button-drawer-filters
    actions:
      openDrawer:
        - id: compositions-page-button-drawer-filters
          resourceRefId: compositions-page-button-drawer-filters
          type: openDrawer
          title: Compositions Filters
  resourcesRefs:
    items:
      - id: compositions-page-button-drawer-filters
        apiVersion: widgets.templates.krateo.io/v1beta1
        resource: filters
        name: compositions-page-button-drawer-filters
        namespace: {{ .Release.Namespace }}
        verb: GET